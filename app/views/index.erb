<h1>Welcome to Flash Cards</h1>
<h2>Pick a deck:</h2>

<div id="deck">
  <ol>
    <% @deck.each do |deck| %>
      <li> 
        <a href="/deck/<%= deck.id %>"><%= deck.name %></a>
      </li>
    <% end %>
  </ol>
<%= erb :_login %>
<%= erb :_signup %>
</div>

